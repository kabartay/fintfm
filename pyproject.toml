[project]
name = "fintfm"
version = "0.0.1"
description = "A tabular foundation model for financial risk — pretrained on synthetic company financials, so it predicts credit default with no training on your data"
readme = "README.md"
authors = [
    { name = "Mukharbek Organokov", email = "mukharbek.organokov@gmail.com" }
]
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.13"
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Financial and Insurance Industry",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
dependencies = [
    "numpy>=2.0",
    "pandas>=2.2",
    "scikit-learn>=1.5",
    "scipy>=1.18.1",
    "torch>=2.4",
]

[project.optional-dependencies]
bench = ["lightgbm>=4.0"]

[project.scripts]
fintfm-train = "fintfm.train:main"
fintfm-bench = "fintfm.bench:main"
fintfm-ablate = "fintfm.experiments:main"

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.6"]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.pytest.ini_options]
testpaths = ["tests"]
